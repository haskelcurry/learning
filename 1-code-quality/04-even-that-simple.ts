const author = users.find(u => u.id === authorId);








// const author = users.find(where({id: authorId}));










// const author = find(users, {id: authorId});










// const getAuthor = find({id: authorId});
